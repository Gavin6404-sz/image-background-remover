# Image Background Remover - MVP 需求文档

## 1. 项目概述

- **项目名称**：Image Background Remover
- **项目类型**：在线图片处理工具（纯工具型网站）
- **核心功能**：上传图片，自动去除背景，下载结果
- **目标用户**：所有人
- **网址**：待定（Cloudflare Pages 部署后分配）

---

## 2. 功能需求

### 2.1 核心功能

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 图片上传 | 支持拖拽上传、点击上传，支持 JPG/PNG 等主流格式 | P0 |
| 自动去背景 | 上传后自动调用 Remove.bg API 处理，无需用户操作 | P0 |
| 结果预览 | 处理完成后在页面显示原图 vs 去背景效果图（同时显示） | P0 |
| 图片下载 | 一键下载去背景后的图片（默认 PNG 透明背景） | P0 |
| 背景色切换 | 可选纯色背景（白、红、蓝等），默认透明 | P1 |

### 2.2 用户交互流程

```
用户访问页面
    ↓
拖拽/点击上传图片（支持 JPG、PNG）
    ↓
自动调用 Remove.bg API 去背景
    ↓
显示处理结果（原图 + 效果图同时显示）
    ↓
选择背景色（可选，默认透明）
    ↓
下载图片
```

### 2.3 非功能性需求

| 项目 | 要求 |
|------|------|
| 处理速度 | 秒级响应（Remove.bg 通常 1-3 秒） |
| 输出精度 | 与原图一致，无明显锯齿或模糊 |
| 图片限制 | 单张图片建议 ≤ 10MB |
| 响应式 | 移动端/PC 端均可正常使用 |

---

## 3. 技术方案

### 3.1 技术栈

| 层级 | 技术选型 |
|------|---------|
| 前端框架 | React + Next.js (App Router) |
| UI 组件 | shadcn/ui |
| 样式 | Tailwind CSS |
| 状态管理 | React Hooks（简单场景无需 Redux） |
| 后端 | Cloudflare Pages Functions（Node.js） |
| 图片处理 API | Remove.bg |
| 部署平台 | Cloudflare Pages |
| 环境变量 | Remove.bg API Key 存在 Pages Functions 环境变量 |

### 3.2 架构设计

```
┌─────────────────┐      ┌──────────────────────┐      ┌─────────────┐
│   浏览器        │  →   │  Cloudflare Pages    │  →   │ Remove.bg   │
│  (用户上传)     │      │  Pages Functions      │      │    API      │
│                 │  ←   │  (代理, Key 保护)    │  ←   │             │
│  (下载结果)     │      └──────────────────────┘      └─────────────┘
└─────────────────┘
```

### 3.3 API 设计

**Remove.bg 调用（Pages Functions 端）**

```
POST /api/remove-background
Content-Type: multipart/form-data

Body: { image: File }

Response:
{
  "success": true,
  "data": {
    "resultUrl": "data:image/png;base64,..." // base64 编码的结果图
  }
}

Error Response:
{
  "success": false,
  "error": "错误描述"
}
```

### 3.4 目录结构

```
image-background-remover/
├── app/
│   ├── page.tsx              # 首页（上传 + 预览 + 下载）
│   ├── layout.tsx             # 根布局
│   ├── globals.css            # 全局样式
│   └── api/
│       └── remove-background/
│           └── route.ts       # Pages Functions API
├── components/
│   ├── ui/                    # shadcn/ui 组件
│   ├── image-uploader.tsx     # 图片上传组件
│   ├── image-preview.tsx      # 预览对比组件
│   └── background-selector.tsx # 背景色选择组件
├── lib/
│   └── remove-bg.ts           # Remove.bg API 调用封装
├── public/
│   └── ...
├── .env.local                 # 本地环境变量（不提交）
├── NEXT.md                         # 项目说明
├── image-background-remover-mvp.md # MVP 需求文档
└── image-background-remover-spec.md # 技术规格文档
```

---

## 4. UI 设计

### 4.1 页面结构

**首页（单页应用）**

```
┌─────────────────────────────────────────┐
│              Header                      │
│         Image Background Remover         │
├─────────────────────────────────────────┤
│                                          │
│    ┌─────────────────────────────────────┐   │
│    │         (拖拽或点击上传图片)         │   │
│    │                                      │   │
│    │  ┌──────────────┐  ┌──────────────┐  │   │
│    │  │              │  │              │  │   │
│    │  │    原图      │  │   去背图     │  │   │
│    │  │  (可缩放)    │  │  (可缩放)    │  │   │
│    │  │              │  │              │  │   │
│    │  └──────────────┘  └──────────────┘  │   │
│    │      原图            去背图           │   │
│    │                                      │   │
│    │  支持放大/缩小查看细节                 │   │
│    │  (滚轮缩放 + 拖拽平移)               │   │
│    └─────────────────────────────────────┘   │
│                                          │
│    ⚠️ 图片限制：支持 JPG/PNG，单张 ≤10MB  │
│                                          │
│    背景色: ●透明 ○白色 ○蓝色 ○自定义（默认透明）      │
│                                          │
│         [      下载图片      ]           │
│                                          │
│    📌 使用方法                          │
│    1. 上传图片（拖拽或点击）            │
│    2. 自动去除背景                      │
│    3. 选择背景色并下载                  │
│                                          │
├─────────────────────────────────────────┤
│              Footer                      │
│         Powered by Remove.bg             │
└─────────────────────────────────────────┘
```

### 4.2 图片展示区交互

| 功能 | 说明 |
|------|------|
| 同时显示 | 原图与去背图左右并排显示，无需切换 |
| 放大 | 滚轮向上或点击"+"按钮 |
| 缩小 | 滚轮向下或点击"-"按钮 |
| 拖拽平移 | 按住鼠标拖拽，拖动查看图片不同区域 |
| 重置 | 点击"重置"按钮恢复初始缩放比例 |

### 4.3 页面提示信息

| 位置 | 提示内容 |
|------|---------|
| 上传区域 | 支持 JPG/PNG 格式，单张图片最大 10MB |
| 使用说明 | 拖拽或点击上传图片，自动去除背景，一键下载 |
| 处理中 | 显示加载动画 + "图片处理中，请稍候..." |
| 处理完成 | "处理完成！点击下载图片" |
| 下载成功 | 显示下载成功提示 |

### 4.4 设计规范

- **主题**：简洁工具风格，以功能为主
- **配色**：黑/白/灰为主，突出操作按钮
- **字体**：系统默认字体，清晰易读
- **暗色模式**：暂不支持，后续扩展

---

## 5. 环境变量

| 变量名 | 说明 | 必填 |
|--------|------|------|
| `REMOVE_BG_API_KEY` | Remove.bg API Key | 是（生产环境） |

> 本地开发可使用 .env.local，Cloudflare Pages 在设置中添加环境变量。

---

## 6. 里程碑

| 阶段 | 内容 | 产出 |
|------|------|------|
| M1 | 项目初始化，基础 UI 上传/预览功能 | 可演示 Demo |
| M2 | 对接 Remove.bg API，完成核心去背景功能 | 功能可用 |
| M3 | 背景色切换、下载功能优化 | 完成 MVP |
| M4 | 部署上线，绑定自定义域名 | 正式发布 |

---

## 7. 已知限制

1. Remove.bg 免费额度有限（50 次/月），生产环境需购买套餐
2. 图片过大（>10MB）可能导致处理超时
3. 复杂背景或低对比度图片可能影响去背效果
4. 纯前端方案（无用户体系），无法统计使用次数

---

## 8. 后续扩展（不纳入 MVP）

- 用户体系（注册/登录，统计使用次数）
- 批量处理
- API 开放
- 水印/裁剪/旋转等编辑功能
- 历史记录
